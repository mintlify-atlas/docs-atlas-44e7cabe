---
title: useInput
description: Hook for handling user keyboard input
---

A React hook that handles user input. It's a more convenient alternative to using `StdinContext` and listening for `data` events. The callback you pass to `useInput` is called for each character when the user enters any input. However, if the user pastes text and it's more than one character, the callback will be called only once, and the whole string will be passed as `input`.

## Signature

```typescript
const useInput = (inputHandler: Handler, options?: Options) => void
```

## Parameters

<ParamField path="inputHandler" type="(input: string, key: Key) => void" required>
  Callback function that is called when the user enters input.
  
  - `input` - The input string. Empty for non-printable keys.
  - `key` - Object containing information about which keys were pressed.
</ParamField>

<ParamField path="options" type="Options">
  Optional configuration object.
</ParamField>

<ParamField path="options.isActive" type="boolean" default="true">
  Enable or disable capturing of user input. Useful when there are multiple `useInput` hooks used at once to avoid handling the same input several times.
</ParamField>

## Return Value

This hook returns `void`.

## Types

### Key

Handy information about a key that was pressed.

<ResponseField name="upArrow" type="boolean">
  Up arrow key was pressed.
</ResponseField>

<ResponseField name="downArrow" type="boolean">
  Down arrow key was pressed.
</ResponseField>

<ResponseField name="leftArrow" type="boolean">
  Left arrow key was pressed.
</ResponseField>

<ResponseField name="rightArrow" type="boolean">
  Right arrow key was pressed.
</ResponseField>

<ResponseField name="pageDown" type="boolean">
  Page Down key was pressed.
</ResponseField>

<ResponseField name="pageUp" type="boolean">
  Page Up key was pressed.
</ResponseField>

<ResponseField name="home" type="boolean">
  Home key was pressed.
</ResponseField>

<ResponseField name="end" type="boolean">
  End key was pressed.
</ResponseField>

<ResponseField name="return" type="boolean">
  Return (Enter) key was pressed.
</ResponseField>

<ResponseField name="escape" type="boolean">
  Escape key was pressed.
</ResponseField>

<ResponseField name="ctrl" type="boolean">
  Ctrl key was pressed.
</ResponseField>

<ResponseField name="shift" type="boolean">
  Shift key was pressed.
</ResponseField>

<ResponseField name="tab" type="boolean">
  Tab key was pressed.
</ResponseField>

<ResponseField name="backspace" type="boolean">
  Backspace key was pressed.
</ResponseField>

<ResponseField name="delete" type="boolean">
  Delete key was pressed.
</ResponseField>

<ResponseField name="meta" type="boolean">
  [Meta key](https://en.wikipedia.org/wiki/Meta_key) was pressed.
</ResponseField>

<ResponseField name="super" type="boolean">
  Super key (Cmd on Mac, Win on Windows) was pressed.
  
  Only available with kitty keyboard protocol.
</ResponseField>

<ResponseField name="hyper" type="boolean">
  Hyper key was pressed.
  
  Only available with kitty keyboard protocol.
</ResponseField>

<ResponseField name="capsLock" type="boolean">
  Caps Lock is active.
  
  Only available with kitty keyboard protocol.
</ResponseField>

<ResponseField name="numLock" type="boolean">
  Num Lock is active.
  
  Only available with kitty keyboard protocol.
</ResponseField>

<ResponseField name="eventType" type="'press' | 'repeat' | 'release' | undefined">
  Event type for key events.
  
  Only available with kitty keyboard protocol.
</ResponseField>

## Example

```tsx
import {useInput} from 'ink';

const UserInput = () => {
  useInput((input, key) => {
    if (input === 'q') {
      // Exit program
    }

    if (key.leftArrow) {
      // Left arrow key pressed
    }
  });

  return …
};
```